services:
  emqx:
    build: .
    container_name: emqx
    ports:
      - "1883:1883"
      - "18083:18083"
      - "8883:8883"
    volumes:
      - emqx-data:/opt/emqx/data
    env_file:
      - .env

volumes:
  emqx-data: